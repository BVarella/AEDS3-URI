#include <stdio.h>

int main(){

	int numTestes, numCidades, numVisiveis, a, b;
	int i;
	int cidades[100];

	scanf("%d", &numTestes);
	for(i = 0; i < numTestes; ++i){
		scanf("%d %d", &numCidades, &numVisiveis);
		
	}

	return 0;
}